/**
 * lib/memory.ts — re-exports from /core to preserve existing import paths.
 * All canonical definitions live in @/core/memory.
 */
export { recallProbability, updateStability } from "@/core/memory";
